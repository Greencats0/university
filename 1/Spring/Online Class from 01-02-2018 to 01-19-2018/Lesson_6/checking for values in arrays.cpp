#include <algorithm> // for std::find
#include <iterator> // for std::begin, std::end
#include <string>

int main (){
  string names[2] = { "Jared", "Helena" };
  string value = "Helena";
  bool in_array(const std::string &value, const std::vector"Helena" &names){
    return std::find(names.begin(), names.end(), value)
  }
}
