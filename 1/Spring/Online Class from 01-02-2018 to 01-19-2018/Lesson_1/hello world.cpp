#include <iostream>
using namespace std;

int main() {
  cout <<"Hello World\n";
  // is the declaration of using namespace the same as when in python "import math as m" where you can then do things like print(m.cos(90))?
  std::cout <<"Using namespace std" <<'\n';
  return 0;
}
